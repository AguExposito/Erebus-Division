using UnityEngine;

public class AgentCharacter : Agent
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Awake()
    {
        RandomStats();
    }

    // Update is called once per frame
    void Update()
    {

    } 
}
